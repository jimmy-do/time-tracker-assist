# server.port=8081

# Allows Spring Boot to create our Schema (tables, prim keys fk, etc.) in our DB for us
spring.jpa.hibernate.ddl-auto=create-drop

# Mac
# spring.datasource.url=jdbc:mysql://localhost:3306/spring_db?serverTimezone=EST5EDT
# spring.datasource.username=root
# spring.datasource.password=Root

# Windows
spring.datasource.url=jdbc:mysql://localhost:3306/time_tracker_assist_db
# ^ Change springvideogame_db to whatever you named your db to
spring.datasource.username=root
spring.datasource.password=root

#showing SQL nicely in Spring Boot console, when transactions execute
spring.jpa.show-sql=true
spring.jpa.properties.hibernate.format_sql=true